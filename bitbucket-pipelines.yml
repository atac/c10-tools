image:
    name: mcferrill/node-python-git:py3
    username: $DOCKER_USER
    password: $DOCKER_PASS
    email: mcferrill@msn.com

pipelines:
    default:
        - step:
            caches:
                - pip
            script:
                - pip install tox
                - tox -e py37-c10,py37-i106
